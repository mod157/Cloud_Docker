CREATE TABLE info
(
  id INTEGER AUTO_INCREMENT,
  student_id INTEGER,
  name VARCHAR(10),
  email VARCHAR(30),
  PRIMARY KEY (id)
);

Insert into info (student_id, name, email)
values(32121849, 'LeeSunjae', 'Nammu8395@gmail.com');
